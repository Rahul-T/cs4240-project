/* OR AND and multiple compare testing*/
main
let
  var x : int;
in
begin
    while(x != 0) do
        if 1 > 2 & 3 < 4 then
           x := 1;
        else if 5 >= 6 | 7 <= 8  then
           x := 2;
        endif; endif;
    enddo;
end
